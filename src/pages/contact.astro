---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
---

<Layout title="Contact">
  <main class="w-full flex flex-col items-center px-6 md:px-10 lg:px-12 h-dvh">
    <div class="flex flex-col w-full max-w-[1300px] relative h-full">
      <Header />
      <section class="mt-24 md:mt-28 lg:mt-32 flex flex-col items-center">
        <p class="text-3xl md:text-5xl lg:text-6xl text-primary font-medium">
          Contact me
        </p>

        <form
          class="flex flex-col gap-4 mt-4 min-w-[300px] md:min-w-[500px] items-center"
        >
          <input
            autofocus
            class="min-w-[300px] max-w-[300px] md:min-w-[500px] md:max-w-[400px] rounded-md px-2"
            required
            minlength="3"
            placeholder="Name"
            type="text"
          />
          <input
            class="min-w-[300px] max-w-[300px] md:min-w-[500px] md:max-w-[400px] rounded-md px-2"
            required
            minlength="3"
            placeholder="Subject"
            type="text"
          />
          <textarea
            class="min-w-[300px] max-w-[300px] md:min-w-[500px] md:max-w-[400px] min-h-[100px] max-h-[300px] rounded-md px-2 [field-sizing:content]"
            required
            placeholder="Message"></textarea>
          <div class="w-full flex justify-end">
            <button
              class="text-background bg-primary py-2 px-4 rounded-md hover:bg-primary/85 hover:scale-110 transition-all"
              >Send</button
            >
          </div>
        </form>
      </section>
    </div>
  </main>
</Layout>
